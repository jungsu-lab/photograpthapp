name: framefit
description: "A local-first mobile photo preset editor."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Device input, non-destructive local processing, and export.
  image_picker: ^1.2.3
  camera: ^0.12.0+2
  image: ^4.9.1
  path_provider: ^2.1.6
  gal: ^2.3.2
  share_plus: ^13.2.0
  shared_preferences: ^2.5.5
  app_settings: ^7.0.0
  sensors_plus: ^7.1.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  permission_handler: ^12.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.30.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/composition/backlit-silhouette.jpg
    - assets/images/composition/clean-product.jpg
    - assets/images/composition/diagonal-product.jpg
    - assets/images/composition/flash-portrait.jpg
    - assets/images/composition/foreground-depth.jpg
    - assets/images/composition/frame-within-frame.jpg
    - assets/images/composition/group-triangle.jpg
    - assets/images/composition/leading-lines.jpg
    - assets/images/composition/low-angle-full-body.jpg
    - assets/images/composition/mirror-selfie.jpg
    - assets/images/composition/negative-space.jpg
    - assets/images/composition/reflection-city.jpg
    - assets/images/composition/symmetry-portrait.jpg
    - assets/images/composition/symmetry-space.jpg
    - assets/images/composition/table-45.jpg
    - assets/images/composition/thirds-portrait.jpg
    - assets/images/composition/top-down-table.jpg
    - assets/images/composition/travel-back-view.jpg
    - assets/images/composition/wide-friends.jpg
    - assets/images/composition/window-reflection.jpg
    - assets/images/preset-previews/after-rain-glow.jpg
    - assets/images/preset-previews/backlight-recovery.jpg
    - assets/images/preset-previews/cel-sky.jpg
    - assets/images/preset-previews/classic-negative.jpg
    - assets/images/preset-previews/clear-detail.jpg
    - assets/images/preset-previews/clean-portrait.jpg
    - assets/images/preset-previews/compact-ccd.jpg
    - assets/images/preset-previews/convenience-flash.jpg
    - assets/images/preset-previews/cool-portrait.jpg
    - assets/images/preset-previews/deep-contrast.jpg
    - assets/images/preset-previews/high-contrast-monochrome.jpg
    - assets/images/preset-previews/kyoto-film.jpg
    - assets/images/preset-previews/morning-kissa.jpg
    - assets/images/preset-previews/muted-cinema.jpg
    - assets/images/preset-previews/natural-correction.jpg
    - assets/images/preset-previews/peach-portrait.jpg
    - assets/images/preset-previews/rainy-alley.jpg
    - assets/images/preset-previews/soft-light.jpg
    - assets/images/preset-previews/soft-monochrome.jpg
    - assets/images/preset-previews/sunset-memory.jpg
    - assets/images/preset-previews/tokyo-neon.jpg
    - assets/images/preset-previews/warm-skin.jpg
    - assets/images/preset-previews/warm-disposable.jpg

  fonts:
    - family: KimjungchulMyungjo
      fonts:
        - asset: assets/fonts/KimjungchulMyungjo-Bold.otf
          weight: 400
        - asset: assets/fonts/KimjungchulMyungjo-Bold.otf
          weight: 600
        - asset: assets/fonts/KimjungchulMyungjo-Bold.otf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
